# Raw data

Place Hungarian precinct GeoJSON and related source files here.

Large files matching patterns in the root `.gitignore` (for example `*.geojson`, `*.zip`) are not tracked by default. Use Git LFS or an external artifact store if you need versioned large files.
